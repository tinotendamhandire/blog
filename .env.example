# used by docker-compose.yml. Copy to .env on the box and fill in real
# values — generate each with `openssl rand -hex 32`. Keep them different
# from each other: a leaked admin secret shouldn't also be a file-host
# write secret.
UPLOAD_SECRET=
ADMIN_SECRET=
