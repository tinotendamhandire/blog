# reference-only clone, not part of this project
/tinotools-reference/

# node
node_modules/
dist/
.astro/
.env
.env.*
!.env.example

# regenerated fresh from git HEAD before every blog image build — never
# meant to be committed itself
/.deploy-context/

# copied fresh from node_modules/@coderline/alphatab/dist into public/ by
# the alphaTab vite plugin on every build/dev run — not source, don't commit
/public/font/
/public/soundfont/

# os
.DS_Store
